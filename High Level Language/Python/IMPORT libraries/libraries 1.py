#importing functions from a library called 'import'
import random
#'random.choice()' is a function present inside the library, 'random'.
#'random.choice()' basically, chooses randomly the between the given arguments
coin = random.choice(["heads", "tails"])
print(coin)
#go to libraries 2.py